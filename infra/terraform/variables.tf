# ---------------------------------------------------------------------------
# Variables are defined in each cloud-specific entry-point directory:
#
#   infra/terraform/gke/variables.tf
#   infra/terraform/eks/variables.tf
#   infra/terraform/aks/variables.tf
#
# This file is intentionally empty — the root directory is not a deployable
# Terraform root module.  See main.tf for usage instructions.
# ---------------------------------------------------------------------------
